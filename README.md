# Landing Page of Ambulink

## Getting started
1. install node
2. install dependencies 
    ```shell
    npm install
    ```
3. Build the CSS 
    ```shell
    npm run build
    ```
4. Start development mode
    ```shell
    npm run watch
    ```
